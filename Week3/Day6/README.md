# Day 6 - Mini Project: Student Performance Dashboard

## Objective

To apply the concepts learned throughout Week 3 by creating a complete data visualization dashboard. The project focuses on exploring a real-world dataset, analyzing the data, creating multiple visualizations, and drawing meaningful conclusions from the results.

---

## Project

### Student Performance Dashboard

### Dataset Used

* Student Performance Dataset
* *(Alternative datasets: Titanic Dataset or Iris Dataset)*

---

## Tasks Performed

### Data Analysis

* Loaded the dataset using Pandas
* Explored the dataset structure
* Checked data types and summary statistics
* Identified missing values

### Data Visualization

Created the following visualizations:

* Line Chart
* Bar Chart
* Histogram
* Pie Chart
* Scatter Plot
* Heatmap (Correlation Matrix)

### Data Exploration

* Examined relationships between variables
* Compared student performance across subjects
* Identified trends and patterns in the dataset

---

## Functions and Libraries Used

### Pandas

* `pd.read_csv()`
* `head()`
* `info()`
* `describe()`
* `isnull()`

### Matplotlib

* `plt.plot()`
* `plt.bar()`
* `plt.hist()`
* `plt.pie()`
* `plt.scatter()`
* `plt.title()`
* `plt.xlabel()`
* `plt.ylabel()`
* `plt.legend()`
* `plt.grid()`
* `plt.show()`

### Seaborn

* `sns.heatmap()`

---

## Files

* Mini_Project.py
* Student_Dataset.py

---

## Conclusions

From the analysis, I was able to:

* Identify the subject with the highest average marks.
* Determine the best-performing students based on their overall performance.
* Visualize the distribution of marks using histograms.
* Compare subject-wise performance using bar and pie charts.
* Analyze relationships between variables using scatter plots.
* Interpret the correlation matrix using a heatmap to identify positive and negative correlations.

---

## Key Learnings

* Data visualization transforms raw data into meaningful insights.
* Different chart types are suitable for different analytical purposes.
* Heatmaps simplify the interpretation of relationships between numerical variables.
* Combining multiple visualizations provides a comprehensive understanding of a dataset.
* Exploratory Data Analysis (EDA) is an essential step before building Machine Learning models.

---

## Outcome

After completing Day 6, I can:

* Load and explore real-world datasets
* Perform basic Exploratory Data Analysis (EDA)
* Create professional visualizations using Matplotlib and Seaborn
* Interpret charts to draw meaningful conclusions
* Build a complete visualization dashboard to communicate data insights effectively
